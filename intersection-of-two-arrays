class Solution:
    def intersection(self, nums1: List[int], nums2: List[int]) -> List[int]:
        res=[]
        for char in set(nums1):
            if char in nums2:
                res.append(char)
        return res
